
<% if (msg_sucesso != '') { %>
    <div class="alert alert-success">
        <%= msg_sucesso %> 
    </div> 
<% } %>

<% if (msg_error != '') { %>
    <div class="alert alert-danger">
        <%= msg_error %> 
    </div>
<% } %>

